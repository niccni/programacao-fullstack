// Usando objetos, crie uma função que calcula a área de um retângulo com base na largura e altura.
const areaRetangulo = {
    largura: 6,
    altura: 10,
}

function Calculo(){
    let total = areaRetangulo.altura * areaRetangulo.largura;
    return total;
}
console.log(Calculo())

